# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001/S01 | process | Feature branch strategy | `feat/room-first-ux` from upstream 2f097aa | Clean slate — no prior code carried. Cherry-pick infrastructure only. | No | agent |
| D002 | M001/S01 | arch | npm lockfile generation | Use npm 10.8.2 via `npx -y npm@10.8.2 install` | Docker node:20-alpine has npm 10.8.2. npm 11 lockfiles cause "Exit handler never called" in Docker builds. | Yes — when Docker base image upgrades npm | agent |
| D003 | M001/S01 | arch | Docker build network | `network: host` for compose build | Docker default bridge DNS can't resolve registry.npmjs.org on this machine. | Yes — environment-specific | agent |
| D004 | M001/S01 | arch | Dockerfile workspace node_modules | Remove per-workspace node_modules COPY | npm workspaces hoist all deps to root node_modules. Per-workspace dirs don't exist. | No | agent |
| D005 | M001/S01 | arch | Test infrastructure source | Port from prior milestones, strip non-upstream additions | MockReadTransport, MockWriteClient, testApp are battle-tested. Stripped mqttClient (not in upstream ServerDependencies). | No | agent |
| D006 | M001/S01 | arch | HA version pinning | Pin HA container to 2026.2 | Bootstrap script and onboarding API tested against this version. | Yes — bump when needed | agent |
| D007 | M003 | process | Branch strategy for M003 | Sub-branches of `feat/multi-device-rooms` using double-dash: `feat/multi-device-rooms--M003-S02` etc. | Git refs can't have a branch be both a file and directory, so `/` separators don't work when `feat/multi-device-rooms` exists. Double-dash separator avoids the conflict. `feat/multi-device-rooms` is the integration branch. | No | agent |
| D008 | M003/S02 | arch | showDevice condition for multi-sensor rooms | showDevice true when room has sensors[] even without legacy deviceId field | Multi-sensor rooms created via the sensors[] API don't have a top-level deviceId. Without this condition, radar cones wouldn't render. The check is `!!selectedRoom.deviceId \|\| (selectedRoom.sensors?.length ?? 0) > 0`. | Yes | agent |
| D009 | M003/S03 | frontend | Sensor removal and legacy field sync strategy | Shared removeSensor callback splices from sensors[], syncs singular fields from sensors[0], clears all legacy fields when last sensor removed | Both trash icon and DeviceEditor onUnlink need the same removal logic. Keeping singular legacy fields in sync with sensors[0] maintains backward compat with any code still reading deviceId/profileId/devicePlacement. | Yes | agent |
| D010 | M004 pre-work | pattern | Unified item editor visibility pattern | Removed showDeviceEditor state, device editor now gates on selectedSensor derived from selectedItem (same pattern as furniture/door/zone editors) | Device editor had a separate showDeviceEditor boolean while all other item editors derived visibility from selectedItem. User identified this as unnecessary special-casing. All item editors now use the same pattern: selectedItem drives visibility, onClose clears selectedItem. | No | collaborative |
| D011 | M004/S01/T02 | architecture | Backend isPointInPolygon duplication vs shared module | Duplicated 15-line ray-casting isPointInPolygon in backend zoneCoverage.ts rather than creating a shared module between frontend and backend | Frontend is ESM/browser, backend is CommonJS/Node. Sharing a 15-line pure function across runtimes isn't worth the build complexity. Both implementations use the same ray-casting algorithm. | Yes | agent |
| D012 | M004/S02/T04 | architecture | HA entity_id derivation from MQTT discovery | HA derives entity_id from object_id field, not unique_id. Expected entity IDs are binary_sensor.zone_N_occupancy, not binary_sensor.ep_room_roomid_zone_N_occupancy. | Discovered during integration testing against live HA. The unique_id is for internal HA registry deduplication; the object_id determines the default entity_id. S04 must use this when looking up entities after creation. | Yes | agent |
| D013 | M004/S03 | architecture | ZoneWriter multi-sensor write strategy | Add new `applyTranslatedZones()` method that accepts DeviceZoneRect/DeviceZonePolygon with explicit slot indices, rather than modifying existing `applyZones()`/`applyPolygonZones()` | Existing methods use ID-based slot matching (extractZoneIndex from zone.id) which doesn't work for multi-sensor assignment where the same room zone maps to different slots on different devices. New method uses explicit slot indices from ZoneAssignment.slotId and writes begin/end coordinates directly (no x+width conversion). Existing methods remain unchanged — still used by single-device ZoneEditorPage and WizardPage flows. | Yes | agent |
| D014 | M004/S03/T02 | architecture | Zone write failure in handleSaveRoom is non-fatal | Room data persists even when zone writes fail. Zone write errors logged to console; S05 will add UI for warnings/unassigned zones. | Room geometry is the source of truth — losing it because zone writes failed would be worse UX. Zone writes can be retried via re-save. Decoupling ensures room data safety. | Yes | agent |
| D015 | M004/S04 | architecture | Room device lifecycle orchestrator placement and assignment data flow | Separate `roomDeviceLifecycle.ts` service in domain/ with pure `buildRoomDeviceDescriptor` function. Apply-zones route re-runs zone assignment to get ZoneAssignment[] for the lifecycle orchestrator rather than modifying RoomZoneOrchestrator's return type. | RoomZoneOrchestrator (S03) returns DeviceWriteResult[] which lacks the raw ZoneAssignment data needed by the lifecycle orchestrator. Rather than changing S03's public interface (risking regressions), the apply-zones route runs the pure assignment functions independently. The coverage + assignment computation is stateless and fast — running it twice has negligible cost. This keeps S03's contract stable and the lifecycle orchestrator cleanly separated from the zone write pipeline. | Yes | agent |
| D016 | M004/S05 | architecture | Where to store overlap threshold — per-zone vs per-room | Per-zone on ZoneRect/ZonePolygon (`overlapThreshold?: number`, 0-1, default 0.1) alongside the existing `aggregationMode` field | R002 says "configurable per zone" and "threshold stored per zone on RoomConfig." Per-zone gives more granular control (a large zone might want lower threshold than a small precise zone). Matches aggregationMode's per-zone placement. Backend's assignZonesToDevices already takes threshold as parameter — future work can read it from each zone. Frontend-only change for S05. | Yes | agent |
